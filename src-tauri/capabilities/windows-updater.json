{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "windows-updater",
  "description": "Windows-only updater and process permissions for the main window",
  "windows": ["main"],
  "permissions": [
    "process:default",
    "updater:default"
  ],
  "platforms": ["windows"]
}
